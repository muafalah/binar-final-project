import React from 'react'
import Layout from '../Layout'

const Homepage = () => {
    return (
        <Layout.Default>
            Homepage
        </Layout.Default>
    )
}

export default Homepage